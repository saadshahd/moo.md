# Roadmap

Forward-looking only. Shipped history lives in `CHANGELOG.md`.

## Active

_Nothing in flight — add current work here._

## Next

- **Agentic memory MCP server** — deferred. Auto-memory now ships as hooks (`memory-prime` foreground framing, `memory-write` off-thread writer) plus `hope/skills/memory.md`. The larger vision — external store, valence-tagged retrieval, configurable ranking, session ingestion — remains unbuilt. Revisit only if the native + hook memory layers prove insufficient.
- **Shape eval calibration** — evals exist at `hope/skills/intent/evals` and `hope/skills/shape/evals`. Open question: does the structured shape workflow (dimensions, expert consult, pre-mortem) measurably beat unguided shaping? Run against real sessions and decide whether to adjust the skill or accept current behavior.
