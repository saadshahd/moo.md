.DS_Store
prompts

# Dependencies
node_modules/
bun.lockb

# Build outputs
dist/

# Local
*.local.md

# Eval workspaces
workspace/

# Wayfinder maps and mined corpora — untracked by decision (22), ignored so
# `git add .` cannot publish verbatim session text to a public repo
.scratch/

# Riptide artifacts (cloud-synced)
.humanlayer/tasks/
