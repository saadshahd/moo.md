{
  "tags": [
    "react",
    "db",
    "distributed"
  ],
  "notes": "distributed: app/api/webhooks/route.ts is a Stripe webhook handler verifying stripe.webhooks.constructEvent signatures and reconciling subscription/product/price state from Stripe's async event stream (server + Stripe + user's browser are three reconciling parties over time). utils/stripe/server.ts's checkoutWithStripe() calls stripe.checkout.sessions.create(...) and createStripePortal() calls stripe.billingPortal.sessions.create(...) — both durably create real remote Stripe objects (a Checkout Session, a Billing Portal session), not read-only lookups. This is the canonical side-effectful-remote-boundary pair (charge/subscription creation + webhook confirmation) the distributed tag targets. db: utils/supabase/queries.ts and utils/supabase/server.ts show owned Postgres access via Supabase (direct .from(table).select(...) queries, not a generic hosted-API consumer), corroborated by schema.sql's full table/RLS definitions and by the webhook route writing subscription/product/price rows on every relevant Stripe event. react: components/ui/AccountForms/CustomerPortalForm.tsx is a 'use client' component using useState/useRouter and rendering JSX; app/account/page.tsx is the server component that feeds it. No workspace/monorepo structure (single package.json at repo root, no pnpm-workspace.yaml or turbo.json) so glob values are left at corpus defaults unscoped. Repo has zero test files (grepped tree.txt for *.test.*/*.spec.*/__tests__, zero hits, and no test runner in package.json devDependencies) so the test glob is left at the corpus's {ts,tsx} default, unverified against a real convention — same caveat pattern as fullstack-next-db. Repo is archived (Vercel stopped actively maintaining it) but was chosen anyway as the canonical, highest-star (7722), MIT-licensed public specimen of the Stripe-webhook-plus-Prisma/Supabase pattern the fixture spec calls for; nothing about being archived changes the correctness of the tag evidence in the files included.",
  "rules": {
    "must_install": [
      "idempotent-handler-by-dedup-key",
      "remote-call-has-a-third-outcome",
      "external-call-timeout",
      "integration-point-isolation",
      "push-work-to-the-database",
      "parse-dont-validate-at-every-untyped-boundary",
      "no-behavioral-boolean-props",
      "stale-message-is-dropped-by-version"
    ],
    "must_not_install": [
      "no-shared-mutable-state-across-workers",
      "replicated-state-is-a-deterministic-fold",
      "restart-policy-as-data",
      "per-unit-crash-isolation",
      "bulkhead-resource-partitioning",
      "actor-isolation-only-for-independent-lifecycle"
    ]
  },
  "rules_notes": {
    "idempotent-handler-by-dedup-key": "+ app/api/webhooks/route.ts — Stripe webhook POST handler dispatches effectful upsert/delete writes on at-least-once-delivered events",
    "remote-call-has-a-third-outcome": "+ utils/stripe/server.ts — checkoutWithStripe awaits stripe.checkout.sessions.create; no unknown/timeout arm",
    "external-call-timeout": "+ utils/stripe/config.ts — Stripe client constructed with no timeout; outbound calls carry none",
    "integration-point-isolation": "+ app/api/webhooks/route.ts — raw stripe SDK called directly from multiple modules, not behind one adapter",
    "push-work-to-the-database": "+ utils/supabase/queries.ts — direct owned-Postgres queries via .from().select().eq().in().order()",
    "parse-dont-validate-at-every-untyped-boundary": "+ app/api/webhooks/route.ts — webhook bodies cast `event.data.object as Stripe.*` at the untyped boundary, no parse step",
    "no-behavioral-boolean-props": "+ components/ui/AccountForms/CustomerPortalForm.tsx — Button rendered with behavioral boolean prop loading={isSubmitting}",
    "no-shared-mutable-state-across-workers": "- no concurrent worker pool; only module-scope value is a frozen const Set",
    "replicated-state-is-a-deterministic-fold": "- no event-sourced/replicated fold; state lives in Postgres, written per-event",
    "restart-policy-as-data": "- no queue/job supervisor; no retry/backoff/dead-letter surface",
    "per-unit-crash-isolation": "- no batch loop over independent units; webhook handles one event per request",
    "bulkhead-resource-partitioning": "- no connection/thread/concurrency pools constructed",
    "actor-isolation-only-for-independent-lifecycle": "- no reducers/actors/message-addressed sub-flows",
    "stale-message-is-dropped-by-version": "+ app/api/webhooks/route.ts — Stripe delivers webhooks out of order; handler writes subscription state from event payloads (labeler ruling 2026-07-10, flipped from must_not)"
  }
}
