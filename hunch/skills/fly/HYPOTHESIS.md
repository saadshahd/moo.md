With `fly` as the way a thought is flown by several agents at once, it lands with every detail accounted for by the human and no change the log cannot explain, on whatever medium the project and harness already have.

Graduate when, across ≥5 real flights on ≥2 different media — real meaning work that would have been done anyway: no skip found after landing (a detail the human has to go back for); no magic (a change the human cannot trace to a part from the log alone); and no flight costs more than 5 of the human's turns, counting answering stopped agents and accounting for details. Kill if any fires on a single flight. Kill also if an agent lands the flight itself and the human accepts it — then the landing check is ceremony.

## Carried from soloflow

`fly` is `soloflow` redesigned (2026-09-05) and inherits its one open bet unmeasured: that an agent briefed at takeoff stops at the edge of its part, where one handed the brief mid-flight runs past it. Across 110 spawned lanes exactly one ever stopped, and that one was a probe. The retrofit check is still the cheapest thing to do: hand one running agent the brief and see.

## Field notes

### 2026-09-05 — founding, unrun

Redesigned from `soloflow`, whose record is `hunch/skills/soloflow/HYPOTHESIS.md` in git history before 0.1.1. What changed:

- the vocabulary is the thought's — thought, destination, part, log, magic, skip — over lane, scope, contract, brief, record
- the medium is whatever the project and harness already have, found before anything else, over Solo's `spawn_agent` and scratchpad
- the log is what the medium already keeps, over a scratchpad written for the flight
- the destination is stated where the human can see it and corrected by them on look, over asked for a yes: `roster` measured yes as the cheapest move, and `soloflow` asked nothing before spawn
- the flight lands only when the human has accounted for every detail, over an agent suggesting its own termination
- no mechanics section: cost, cache, worktree setup and model tier are the pilot's to know, not the skill's to lecture
- no mental-model elicitation file: the cut into parts is the pilot's and is shown in the log, never asked, since the "seam" ruling was unanswerable by anyone
