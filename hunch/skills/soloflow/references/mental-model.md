Extract the user's mental model. Two cases based on what you could infer which:

- IF "user" has a mental model already?
  - Draw it out.
  - What are the pieces?
  - Where do the boundaries fall? what must not leak across them?
  - What split would the user refuse?

- ELSE "user" has no mental model yet?
  - Help build one.
  - Name the tensions you found in the ground.
  - Put candidate decompositions that differ _structurally_ rather than cosmetically.
  - Let the user's reaction to the differences be the model forming.
