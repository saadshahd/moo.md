With `plain` loaded, prose outputs pass a plain-style spot-check by default — no stock figure of speech, no long word where a short one does, no passive where active works, no zombie noun, no hedge, no sentence over the STE caps, no synonym drift, no point made twice — while code and technical terms come through untouched.
